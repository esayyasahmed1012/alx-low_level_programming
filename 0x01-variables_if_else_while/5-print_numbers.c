#include <stdio.h>
/**
 * main - entery point
 *
 * Return: always 0 (success)
 */
int main(void)
{
int digit;
for (digit = 0; digit < 10; digit++)
{
printf("%digit", digit);
}
printf("\n");
return (0);
}
