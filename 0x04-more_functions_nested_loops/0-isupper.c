#include "main.h"
/**
 * _isupper - checks if the lettter is upper or lowercase
 * @c: print integer
 * Return: returns 0 or 1
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
